[project]
name = "l2flow-arrow-hot-reader"
version = "0.1.0"
description = "Python Arrow and Redis latest-value readers for L2Flow-CH"
requires-python = ">=3.11,<3.15"
dependencies = [
    "polars>=1.30,<2",
    "pyarrow==25.0.0",
    "redis[hiredis]>=6,<8",
]

[tool.uv]
package = false
